{
  "version": 2,
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/tododolix"
      }
    }
  ],
  "routes": [
    {
      "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
    }
  ]
}
